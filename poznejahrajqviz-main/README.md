# Poznej a Hraj Qviz

Realtime quiz experience built with Vite + React. Includes Firebase-ready scaffolding for creating and joining quiz rooms. Use this foundation to extend gameplay, lobby management, scoring, and live question control.

## Development

```bash
npm install
npm run dev
```

Copy `.env.example` to `.env` and add your Firebase credentials to enable Firestore interactions.
